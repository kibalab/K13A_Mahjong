 
using UdonSharp;
using UnityEngine;
using VRC.SDKBase;
using VRC.Udon;

public class CardStash : UdonSharpBehaviour
{
    /*
    public CardComponent[] StashedCards = new CardComponent[70];
    public CardComponent lastedStashedCard;

    public void SetStashCard(CardComponent card)
    {
        lastedStashedCard = card;
        if (lastedStashedCard != null)
        {
            StashedCards[StashedCards.Length] = lastedStashedCard;
        }
    }
    */
}
